version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Nada que construir, es un sitio estático"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  rewrites:
    # Esta regla reescribe la ruta /api a su backend Node.js
    - source: '/api/<*>'
      target: 'https://URL-FIJA-DE-SU-BACKEND.com/api/<*>'  # <-- ¡REEMPLAZAR ESTO!
      status: 200